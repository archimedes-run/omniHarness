# Gate A whitelist — agent-construction sites that do not reach the shared
# middleware base.
#
# Every entry needs a reason. An entry deferring to future work must name the
# task that closes it, and the gate FAILS once that task lands.

packages/harness/omniharness/agents/factory.py  # BLOCKED, needs a decision — not deferred work. FR-003 offered two closures and BOTH conflict with something real. Routing it through _build_runtime_middlewares breaks the documented middleware-takeover contract (`middleware=[x]` must yield exactly `[x]`) — attempted, and it failed 6 tests in tests/test_create_deerflow_agent.py that assert exactly that. Removing create_omniharness_agent from the public surface closes the bypass for embedders but leaves the create_agent call in place, so this gate as written still flags it. See gate-verification.md.
